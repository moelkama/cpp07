#ifndef ARRAY_HPP
#define ARRAY_HPP

// #include <iostream>
// #include <exception>

template <typename T>
class Array
{
private:
    T               *array;
    unsigned int    si_ze;
public:
    Array();
    // ~Array();
    // Array(unsigned int n);
    // Array(const Array& other);
    // Array& operator=(const Array& other);
    // T&  operator[](long i);
    // unsigned int    size() const
    // {
    //     return (this->size);
    // }
};

#endif